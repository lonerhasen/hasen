n = int(input())
arr = list(map(int, input().split()))

total_bases = 0
at_bats = 0

for x in arr:
    if x == -1:
        continue
    total_bases += x
    at_bats += 1

print(total_bases / at_bats)
